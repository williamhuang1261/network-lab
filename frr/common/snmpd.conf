# Lab-only SNMP config: read-only "public" community, no external exposure
# (each router only listens on its own Docker-internal bridge network).
rocommunity public
agentaddress 1161
syslocation network-lab
syscontact lab@network-lab.example
